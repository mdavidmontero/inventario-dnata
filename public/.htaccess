<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        # Evita listado de directorios y MultiViews
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Manejo de cabeceras necesarias para Livewire y autenticación
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirige URLs con slash final si no son carpetas
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Todas las demás solicitudes se envían al Front Controller
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
